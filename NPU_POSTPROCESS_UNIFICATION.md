# NPU detection 후처리 통일 보고서

- 날짜: 2026-07-14
- 대상: `run_detection_process` 의 NPU 분기
- 검증: 30프레임 정확성 대조 + case_E(8모델) 실행 5회 (NPU 통일후 2, GPU 통일후 2, GPU 구코드 A/B 1)

---

## 0. 요약

| | 통일 전 | 통일 후 |
|---|---|---|
| NPU yolo 후처리 | **243.6 ms** | **7.7 ms** (**31~33배**) |
| NPU yolo 큐잉 대기 | 2,810 ms | 212 ms |
| **y2 (deadline miss)** | **0.9242** | **0.5651 (−38.9%)** |
| y1 (throughput) | 265.2 | 315.2 (+18.8%) |
| y3 (tokens/s) | 14.21 | 16.59 (+16.7%) |
| 검출 정확성 | — | **박스 수 동일, score 차이 0.0000, 좌표 오차 median 0.00px** |

**결론: NPU 후처리는 하드웨어 특성이 아니라 소프트웨어 결함이었다. 통일 완료.**

---

## 1. 진단

### 1.1 CPU/GPU 경로 (`_yolo_decode`)
- **입력**: ONNX raw `(1, 84, 8400)` — 박스는 **xywh**(입력 픽셀), 점수는 **이미 sigmoid**
- **처리**: numpy로 conf 필터 → xywh→xyxy → letterbox 역변환 → `torchvision.ops.nms` (class-agnostic)
- **출력**: `[(x1,y1,x2,y2,score,cls), ...]` — **원본 프레임 좌표**

### 1.2 NPU 경로 (`model.postprocess`)
호출 지점: `model_processors.py` 의 `run_detection_process` NPU 분기.

**NPU raw 출력** (yolo11s, 실측):
```
[0] (20, 20, 64) float32     [1] (20, 20, 80) float32
[2] (40, 40, 64) float32     [3] (40, 40, 80) float32
[4] (80, 80, 64) float32     [5] (80, 80, 80) float32
```
= **디커플드 헤드 raw**. 스케일별로 DFL 박스(64 = 4×reg_max16)와 **클래스 로짓**(80).
ONNX 출력 `(1, 84, 8400)` 과 **포맷이 완전히 다르다** → **어댑터 필요.**

zoo의 decode 규약 (`yolo_anchorless_post.py`):
- 정렬: numel 내림차순 (80×80 → 40×40 → 20×20)
- DFL: reg_max(16) 축 **softmax → 기대값** → ltrb 거리(셀 단위)
- 박스: `dist2bbox(xywh=False)` × stride → **xyxy(입력 픽셀)**
- 점수: **sigmoid**

### 1.3 왜 243ms 였는가 — **알고리즘이 아니라 torch 스레드 오버서브스크립션**

zoo postprocess를 **단독 실행**하면 1.0ms다. 243ms는 경합 상태에서만 나온다.

torch 스레드 수를 바꿔가며 측정 (yolo11s, NPU, 20프레임):

| torch threads | zoo postprocess | npu_detections | draw |
|---|---|---|---|
| **24 (기본 = 코어 수)** | **35.92 ms** | 0.10 ms | 0.39 ms |
| 8 | 1.57 ms | 0.07 ms | 0.28 ms |
| 4 | 1.31 ms | 0.06 ms | 0.32 ms |
| 1 | 1.89 ms | 0.06 ms | 0.29 ms |

- zoo의 postprocess는 **torch CPU 연산**(DFL 디코드·앵커 그리드·NMS)을 8400개 앵커 전체에 건다.
- 워커는 뷰마다 별도 프로세스이고 torch는 각 프로세스에서 **코어 수만큼 스레드**를 띄운다.
  → 8 워커 × 24 스레드 = **192 스레드가 24 코어에서 경합** → 243ms.
- `_yolo_decode`는 numpy로 **먼저 걸러낸 뒤** 살아남은 수백 개 박스에만 NMS를 걸어
  torch 병렬 작업이 거의 없다 → 경합에 둔감했다 (CPU/GPU가 1.3~2.0ms인 이유).

`npu_detections`(0.06ms)와 `_draw_dets`(0.3ms)는 무시할 수준이며, 원인이 아니다.

---

## 2. 수정 내용

### 2.1 어댑터 (`_npu_yolo_raw_to_pred`)
NPU raw 6개 텐서 → ONNX와 **동일한 `(1, 84, N)`** 으로 변환 (numpy):
- DFL softmax → 기대값 → ltrb
- 셀 중심 앵커 + stride → xyxy → **xywh** (ONNX 규약에 맞춤)
- 클래스 로짓 → **sigmoid**

그 뒤 **CPU/GPU와 똑같은 `_yolo_decode`** 를 호출한다. zoo의 `model.postprocess()` 는 detection 경로에서 제거했다. (**classification의 top-k는 건드리지 않았다.**)

letterbox 역변환은 `_letterbox_meta()` 로 CPU/GPU와 동일한 공식을 쓴다.
`conf_thres` / `iou_thres` 는 **양쪽 동일**(0.25 / 0.45) — 워커의 같은 변수를 그대로 전달한다.

### 2.2 torch 스레드 고정 (`_limit_cpu_threads`)
**vision 워커에만** `torch.set_num_threads(1)`.
- detection/classification: torch는 이제 소수 박스의 NMS만 하므로 병렬성은 순수 오버헤드.
- **생성(LLM/VLM) 워커에는 적용하지 않는다** — torch가 곧 엔진이다.
  1스레드로 묶었더니 llama1b(GPU) 생성이 **1030 → 1427ms (+38%)** 로 느려져 되돌렸다.

---

## 3. 정확성 검증 (30프레임, conf=0.25 / iou=0.45 동일)

| 지표 | 결과 |
|---|---|
| 박스 수: zoo(현행) vs 통일디코더 | **13.87 vs 13.87 (동일)** |
| top-1 score 차이 | **0.0000** |
| 좌표 오차 (vs zoo) | **median 0.00 px**, p95 2.08 px, max 26.28 px |
| 클래스+좌표(±2px) 일치율 (vs zoo) | 94.7% |

**꼬리(max 26px)의 정체**: zoo는 클래스별 NMS, `_yolo_decode`는 class-agnostic NMS다.
겹친 중복 박스 중 어느 것을 남길지가 갈린다. **이는 의도된 결과다** — 목표가
"NPU를 CPU/GPU와 같은 후처리로 통일"이므로, NPU는 이제 CPU/GPU와 **같은 NMS**를 쓴다.

### NPU vs CPU 차이는 디코더 탓인가? → **아니다 (int8 양자화 탓)**

| | ONNX(CPU, fp32)와의 일치율 |
|---|---|
| zoo 후처리를 쓴 NPU | 74.6% |
| **통일 디코더를 쓴 NPU** | **75.3%** |

거의 동일 → 디코더 교체가 NPU-CPU 격차를 만들지 **않는다.** 격차의 원인은 NPU 모델이
**int8 양자화**된 것이고(ONNX는 fp32), 이는 하드웨어의 본질적 특성이다.

> BGR→RGB 수정이 적용된 코드에서 비교했다.

---

## 4. 속도 검증 (case_E, 8모델, 180초, NPU)

| 모델 | post 전 | **post 후** | 배수 | infer 전 | infer 후 | wait 전 | wait 후 |
|---|---|---|---|---|---|---|---|
| yolo11s | 242.12 | **7.37** | **32.9×** | 30.97 | 22.49 | 2,781 | 214 |
| yolo11m | 243.03 | **7.74** | **31.4×** | 33.82 | 33.70 | 2,804 | 223 |
| yolo11l | 243.61 | **7.85** | **31.0×** | 34.77 | 37.88 | 2,810 | 180 |
| yolo11x | 244.70 | **7.94** | **30.8×** | 40.70 | 48.30 | 2,840 | 31 |
| resnet50 | 0.21 | 0.23 | — | 13.70 | 10.89 | 145 | 97 |
| mobilenet_v2 | 0.21 | 0.22 | — | 12.59 | 9.45 | 136 | 87 |
| llama1b | — | — | — | 6,693 | **5,152** | 0 | 0 |
| qwen2_vl | — | — | — | 2,122 | 2,137 | 49,223 | **32,166** |

- 후처리 **243ms → 7.7ms** (목표였던 "CPU/GPU 수준 1~2ms대"보다는 크지만, **어댑터 비용 포함** 값이다).
- CPU 부하가 줄어 **다른 모델의 추론도 빨라졌다** (llama1b −23%, resnet50 −21%).
- 큐잉 대기가 2,810ms → 212ms 로 **13배** 감소.

**남은 7.7ms의 정체**: 어댑터의 DFL softmax를 8400 앵커 **전부**에 대해 numpy로 계산한다.
CPU/GPU 경로는 ONNX가 이 디코드를 그래프 안에서 이미 끝내고 오므로 그 비용이 `infer` 에
포함되어 있다. **즉 7.7ms는 "NPU가 느린 것"이 아니라 "ONNX에선 infer로 잡히던 비용이
NPU에선 post로 잡히는 것"이다.** 공정성에는 문제가 없지만, 더 줄이려면 conf 임계값으로
먼저 필터링한 뒤 DFL을 적용하면 된다 (§7).

---

## 5. 파급 확인

### 5.1 y2 포화 완화 ✅ (rate factor 여유 확보)

| case_E | 통일 전 | 통일 후 |
|---|---|---|
| **CPU-NPU y2** | **0.9242** | **0.5651 (−38.9%)** |
| CPU-GPU y2 | 0.3022 | 0.3308 (변화 없음, §5.3) |

**1× rate에서 miss가 0.92 → 0.57 로 내려왔다. rate factor를 2×, 4× 로 올릴 여유가 생겼다.**
(수렴 리포트 §7-b에서 "y2 포화 때문에 rate 범위를 못 늘린다"고 했던 블로커가 해소됐다.)

### 5.2 duration 재확인 ✅ 180초 유지

통일 후 case_E 수렴 시점 (워밍업 20초 제외):

| run | y1 ±2% | y2 ±2% | y3 ±2% |
|---|---|---|---|
| npu rep10 | 110s | 110s | 55s |
| npu rep11 | 125s | 110s | 125s |
| gpu rep10 | 60s | 130s | 55s |
| gpu rep11 | 15s | 165s | 45s |

전부 **165초 이내**. 통일 전 gpu y2가 210~255초였던 것에서 개선됐다. **180초 권장값 유지**
(더 줄일 여지는 있으나, 위 span이 160초라 그 이상은 확인 불가).

**반복 CV (통일 후, NPU)**: y1 1.9%, y2 1.0%, y3 0.5% — 라벨 노이즈 하한이 개선됐다.

### 5.3 ⚠️ GPU 저하는 이 수정 때문이 아니다 (A/B로 확인)

통일 후 GPU 수치가 나빠 보였다 (y1 872→723, llama1b 1097→1427ms). **구코드를 지금 다시
돌려** 확인했다:

| run | llama1b | qwen2_vl | yolo11s | y1 | y2 |
|---|---|---|---|---|---|
| 구코드 (2시간 전, 스윕 초반) | 1030.5 | 478.1 | 12.89 | 924.3 | 0.2909 |
| **구코드 (방금, 동일 상태)** | **1441.8** | **684.9** | **17.89** | **731.6** | **0.3338** |
| 신코드 (통일 후) | 1455.6 | 668.9 | 18.19 | 726.1 | 0.3342 |
| 신코드 (통일 후) | 1398.8 | 674.2 | 17.48 | 719.6 | 0.3275 |

**구코드와 신코드가 같은 시점에 동일한 값을 낸다.** GPU 경로는 손대지 않았으므로 당연하다.
저하의 정체는 **장시간 세션에 걸친 머신 상태 드리프트**다 (수렴 리포트에서 "gpu_case_E의
분산이 duration과 무관하게 6~13%"라 했던 현상의 실체).

> 🔴 **이것은 100시간 수집 계획에 직접 영향을 준다.** GPU 측정값은 **세션을 가로질러 비교
> 불가능**하다. 재수집 시 CPU-GPU 데이터는 (a) 짧은 세션으로 쪼개고 (b) 세션마다 기준
> 조합(anchor)을 재측정해 드리프트를 보정하거나, (c) 조합 순서를 무작위화해야 한다.
> **미해결 이슈로 남긴다.**

### 5.4 배치 순위 변화

| | NPU y2 | GPU y2 | 격차 |
|---|---|---|---|
| 통일 전 | 0.9242 | 0.3022 | **+0.622** |
| **통일 후 (동일 세션)** | **0.5651** | **0.3308** | **+0.234** |

- 통일 전에는 NPU 배치가 "압도적으로 나쁨"으로 보였다 — 그 대부분이 **Python NMS의
  스레드 오버서브스크립션**이었다.
- 통일 후 격차가 **62% → 23%p 로 줄었다.** NPU는 여전히 GPU보다 miss가 많지만
  (RTX 5090 대 MLA100이니 당연하다), 이제 **소프트웨어 핸디캡 없이 하드웨어로 경쟁한다.**
- 추론 단계 비중이 커져(11~15% → 74~85%) **배치가 실제로 성능을 가르기 시작한다** —
  예측기가 배울 신호가 생겼다는 뜻이다.

---

## 6. 남은 이슈

1. **🔴 GPU 세션 드리프트** (§5.3) — 재수집 설계에 반영 필요. 미해결.
2. **어댑터의 7.7ms** (§4) — conf 임계값으로 먼저 필터링한 뒤 DFL을 적용하면 1~2ms로
   줄일 수 있다. 다만 그러면 CPU/GPU(ONNX가 전체 디코드를 미리 함)와 **계산량이 달라져**
   공정성이 흐려진다. 현 상태는 "양쪽 모두 8400 앵커를 전부 디코드"로 대칭이다. 판단 필요.
3. **class-agnostic vs 클래스별 NMS** — 이제 세 디바이스 모두 class-agnostic이다(일관됨).
   정확도를 중시한다면 `_yolo_decode`를 클래스별 NMS로 바꾸되, **세 디바이스에 동시에**
   적용해야 한다.
4. 수렴 리포트의 나머지 블로커(**y1 정의**, y3 마스킹)는 그대로 남아 있다.

---

## 7. 다음 단계

- [x] NPU 후처리 통일 → y2 포화 해소
- [ ] y1 정의 결정 (`1000/추론시간` → 달성 처리량?)
- [ ] y3 마스킹 (LLM 없는 세트)
- [ ] GPU 세션 드리프트 대응책
- [ ] 짧은 재확인 스윕 → **rate factor 확정** (이제 2×/4× 여유 있음) + duration 확정
- [ ] 예산 승인 → 대규모 재수집
