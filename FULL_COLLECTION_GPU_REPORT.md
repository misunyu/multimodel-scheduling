# Phase B — CPU-GPU 본수집 · deploy_cpu_gpu 학습·검증 보고서

- 날짜: 2026-07-17 ~ 07-18
- 수집: 540 조합 (15 working set × 층화/전수 × rate 3단계), **0 실패**, 약 33시간
- 산출: `deploy_cpu_gpu_{y1,y2,y3}.json` + `_features.json` + `_coverage.json`
- 판정: **✅ CPU-NPU와 대칭 · 별도 예측기 유지 근거 확보**
- 레시피: Phase A([[FULL_COLLECTION_NPU_REPORT.md]])와 **동일** (feature 37개, y1=완료건수/창,
  y3 조합마스킹, (models,workload,rate) 정규화, 층화 축·목표, out-of-fold)

---

## 0. 요약

| 항목 | 결과 |
|---|---|
| 수집 | 540/540 조합, 0 실패 |
| GPU 실행 검증 (B.0) | vision 6모델 전부 CUDA EP, 프로세스 GPU 메모리 1–2GB, **CPU 폴백 0** |
| anchor 드리프트 | 30회 **CV 0.109%** (격리 완벽) |
| **S8-GPU 대칭성** | GPU **+0.987** vs NPU +0.991, **Δ=−0.004 → 대칭 ✓** |
| 전체 out-of-fold Spearman | **+0.970** (15세트, 45개 rate 그룹) |
| 플랫폼 최적배치 차이 | **45개 중 30개 그룹**에서 NPU/GPU 최적 배치 상이 |

---

## 1. GPU 실행 검증 (B.0) — 조용한 CPU 폴백 배제

첫 GPU 조합 실행 전, vision ONNX 세션이 실제로 CUDA에서 도는지 3중 신호로 확인
(`gpu_verify.py`): (1) 노드별 EP 배치 카운트, (2) `nvidia-smi`로 이 프로세스의 GPU 메모리,
(3) CPU 대비 속도.

- resnet50 / mobilenet_v2 / yolo11{s,m,l,x}: 노드 **100% CUDA**, 프로세스 GPU 메모리 1–2GB.
- mobilenet_v2는 모델이 워낙 작아(0.86ms) 속도比가 0.9배로 낮지만, 노드 100/0 + GPU 메모리
  1084MB로 **진짜 GPU 실행** 확정 (속도比 단독 판정의 함정 회피).
- 판정: **모든 vision 모델 진짜 GPU 실행 → 수집 진행**.

## 2. 예산·층화 (Phase A와 동일)

정본 `working_sets.yaml` 기준 CPU-GPU 단독 **540 조합**. CPU-GPU 스케줄은 CPU-NPU와
**배치 정렬**(같은 자유배치) 상태로 생성됨 → 플랫폼 간 공정 비교 가능.

## 3. 수집 결과 (B.1)

수집 순서는 NPU와 동일: S8 → S10 → base1~5 (체크포인트) → S7 → S9 → S6 → S5 → S1~S4.
JSONL 증분 저장(크래시 복구), run마다 프로세스 재시작, anchor 20회마다.

| 그룹 | 조합 | 실패 | 소요 |
|---|---|---|---|
| S8 (체크포인트) | 72 | 0 | 269.5분 |
| S10 | 72 | 0 | 268.4분 |
| base1~5 | 120 | 0 | 439.7분 |
| S7 | 60 | 0 | 222.0분 |
| S9 | 48 | 0 | 177.9분 |
| S6 | 48 | 0 | 178.8분 |
| S5 | 48 | 0 | 178.3분 |
| S1~S4 | 72 | 0 | 264.1분 |
| **합계** | **540** | **0** | **~33.3h** |

- **anchor(전부-GPU S3) 30회 CV 0.109%** (y1 137.24–137.92). 격리 완벽 — 33시간 내내
  배경 CPU 부하 0, 측정 신호를 안 덮음.

## 4. 체크포인트 (B.2) — S8-GPU/NPU 대칭성

NPU와 **동일 위치**(S8+S10+base = 264조합) 시점에 정지, 임시 예측기 학습(최종 아티팩트
비파괴), out-of-fold로 정직하게 채점:

| 세트 크기 | out-of-fold Spearman (GPU 체크포인트) |
|---|---|
| 4모델 | +0.965 |
| 7모델 | +0.989 |
| **8모델 (S8)** | **+0.987** |

- **S8-GPU +0.987 vs S8-NPU +0.991 → Δ=−0.004, 대칭.** rate별도 균일
  (rate 1.0/1.5/2.0 → +0.984/+0.986/+0.990) — 반전된 rate 그룹 없음, **비대칭 없음**.
- 레시피가 NPU→GPU로 깨끗이 전이됨을 확인 후 나머지 수집 계속.

## 5. 최종 학습 (B.3)

`deploy_cpu_gpu_{y1,y2,y3}.json` 학습 (기존 아티팩트는 `artifacts/pre_recollect_backup/`로 백업):
- 540행, 37 feature, **view_counts 2~8**, 9모델(mobilenet_v2 포함), 15 model_sets
- y3: LLM/VLM 가속기 배치 조합만 유효 → **372/540 행 학습, 168행 마스킹** (NPU와 동일)
- rate_factors [1.0, 1.5, 2.0, 3.0, 4.0]

## 6. 검증 (B.4) — out-of-fold, 학습에 안 쓴 예측

| 세트 | 모델수 | 평균 Spearman | 범위 |
|---|---|---|---|
| S1 | 2 | +1.000 | [+1.00,+1.00] |
| S2 | 3 | +0.968 | [+0.95,+1.00] |
| S3 | 3 | +0.897 | [+0.88,+0.90] |
| S4 | 3 | +0.933 | [+0.80,+1.00] |
| base1~5 | 4 | +0.937~+0.984 | — |
| S5 | 5 | +0.983 | [+0.97,+0.99] |
| S6 | 6 | +0.973 | [+0.96,+0.98] |
| S7 | 7 | +0.993 | [+0.99,+0.99] |
| S9 | 5 | +0.990 | [+0.98,+1.00] |
| **S8** | 8 | **+0.987** | [+0.98,+0.99] |
| S10 | 7 | +0.994 | [+0.99,+1.00] |

- **전체 45개 (세트,rate) 그룹 평균 +0.970** (NPU +0.954보다 소폭 높음). 15세트 모두 ≥+0.897.
- **S8-GPU +0.987 — NPU +0.991과 대칭.**
- y3 마스킹: vision-only 세트(S1, S2, S9, S10)는 점수식에서 y3 항 제외.

## 7. 플랫폼 비교 (B.5) — NPU vs GPU 최적 배치

측정 점수(정규화 전 raw) argmax로 각 (세트,rate)에서 실측상 최적 가속기 배치를 뽑아 비교
(`compare_platforms.py`):

- **45개 그룹 중 30개에서 두 플랫폼의 최적 배치가 다름.**
- **지배적 패턴: GPU 최적 배치는 거의 항상 llama1b(LLM)를 가속기에 올리고, NPU는 안 올린다.**
  - 예) S3·S5·S6·S7·base1~5: GPU=[…,llama1b,…] / NPU=[… llama1b 제외 …].
  - 해석: GPU는 LLM을 가속기에서 충분히 빠르게 돌려 슬롯 할당이 이득이지만, NPU에서는 LLM을
    가속기에 올리는 이득이 작아 CPU에 남기고 resnet50 등 vision을 올리는 편이 낫다.
- **결론: 하드웨어 특성차가 최적 배치를 실제로 갈라놓는다 → deploy_cpu_npu / deploy_cpu_gpu
  두 예측기를 따로 유지하는 것이 정당.** (vision-only 세트 S1·S2·S9·S10은 LLM이 없어 15개 그룹
  전부 동일 배치 — 차이는 전적으로 생성 모델 배치에서 발생.)

---

## 8. Phase B 판정

| 질문 | 답 |
|---|---|
| GPU가 진짜로 실행되는가 (폴백 없음)? | ✅ 노드 100% CUDA, 프로세스 GPU 메모리 1–2GB |
| deploy_cpu_gpu가 배치 순위를 맞히는가? | ✅ out-of-fold 평균 +0.970 |
| **S8-GPU가 NPU(+0.991)와 대칭인가?** | ✅ **+0.987, Δ=−0.004** |
| 격리로 드리프트 방지? | ✅ anchor CV 0.109% (33시간) |
| 두 예측기 분리가 정당한가? | ✅ 45개 중 30개 그룹에서 최적 배치 상이 (LLM 배치차) |

**→ CPU-NPU + CPU-GPU 두 예측기 모두 본수집·검증 완료. 배치 정렬 스케줄로 공정 비교까지 확보.**
