# multimodel-scheduling-mobilint requirements
# Target: Mobilint Aries NPU + NVIDIA GPU (CUDA) + CPU
#
# NOTE: The Mobilint SDK is not on PyPI. Install it separately per Mobilint's
# instructions (these provide `qbruntime`, `qbcompiler`, `mblt_model_zoo`,
# `mblt_tracker`):
#   pip install mobilint-qb-runtime==1.2.0
#   pip install qbcompiler-<ver>+aries*-py3-none-any.whl        # local wheel from Mobilint
#   pip install -e git+https://github.com/mobilint/mblt-model-zoo.git#egg=mblt_model_zoo
#   pip install -e git+https://github.com/mobilint/mblt-tracker.git#egg=mblt_tracker
#   # mblt-model-zoo LLM/VLM extras:
#   pip install "mblt-model-zoo[transformers]"

# --- Deep learning / inference ---
torch==2.12.0
torchvision==0.27.0
onnx==1.21.0
onnxruntime-gpu==1.20.1
transformers==5.9.0
tokenizers==0.22.2
safetensors==0.7.0
huggingface_hub
accelerate
ultralytics==8.4.56

# --- Placement model ---
xgboost==3.2.0
scikit-learn==1.7.2
pandas==2.3.3
numpy==2.2.6
scipy==1.15.3

# --- App / UI / IO ---
PyQt5==5.15.11
opencv-python-headless==4.13.0.92
Pillow==12.2.0
pyyaml==6.0.3
psutil==7.2.2
matplotlib==3.10.9
tqdm==4.67.3
tabulate==0.10.0
