# 파일럿 수집 정찰 보고서

- 날짜: 2026-07-15
- 성격: **타당성 정찰 (논문 데이터 아님)**. 3세트 × 2플랫폼, 약 3.6시간.
- 수집: CPU-NPU 28 runs (108분) + CPU-GPU 28 runs (108분) + anchor 6회, **0 실패**
- 세트: S1(2, vision), S3(3, LLM), S8(8, 층화16) — `working_sets.yaml`

---

## 0. 판정 — **✅ 전량 수집 진입 권고 (조건부)**

| 정찰 질문 | 결과 |
|---|---|
| 1. 예측 순위 = 실측 순위? | ✅ 5/6 케이스 Spearman **+0.80~+1.00**. S8-NPU만 실패 — **OOD 때문, 접근 실패 아님** |
| 2. NPU vs GPU 최적 배치가 다른가? | ✅ **S3·S8에서 다름** — 플랫폼별 수집의 근거 |
| 3. 새 y1이 배치를 구분하는가? | ✅ **양 플랫폼 층 상관 +0.94/+0.96**, y1 9~22배 변화 |
| 4. y3 마스킹 (S1)? | ✅ vision-only에서 y3 항 제외 확인 |
| 5. **GPU 드리프트가 격리로 잡히는가?** | ✅ **GPU anchor CV 0.35%** (108분). 본수집 최대 리스크 해소 |
| 6. 층화가 배치 공간을 덮는가? | ✅ 0~7 전 층 커버 |

**핵심 근거: 실측 데이터가 완벽하게 정상이다.** S8에서 가속기 배치 모델 수(층)를 0→7로
올리면 실측 y1이 단조 증가(NPU +0.938, GPU +0.962), y2 단조 감소(−0.955, −0.957).
배치가 성능을 명확히 가른다 — **예측 이전의 근본 문제는 없다.**

---

## 1. S8 층화 축 + 선정 16조합

### 축 선정 (기존 배치 변별력 데이터로 정량 평가)

| 축 후보 | y1 설명력 R² | y2 설명력 R² |
|---|---|---|
| **가속기에 배치된 모델 수** | **0.894** | **0.975** |
| llama1b 가속기 여부(단일) | 0.391 | 0.457 |

→ **"가속기 배치 모델 수"를 층화 축으로 확정** (llama1b 단일 위치보다 2배 이상 설명력).

### 선정 (128 → 16, 극단 포함)
자유배치 7개(qwen2_vl은 가속기 고정) → 층 0~7. 극단(전부 CPU=층0, 전부 가속기=층7) 필수 포함,
중간 층은 균등 분배:

```
층 분포: {0:1, 1:3, 2:3, 3:2, 4:2, 5:2, 6:2, 7:1}  (총 16)
```
**CPU-NPU와 CPU-GPU에 동일한 16 배치** 사용 (조합명 정렬, qwen2_vl만 npu↔gpu 차이).

---

## 2. 수집 요약

| 플랫폼 | runs | 실패 | 소요 | anchor |
|---|---|---|---|---|
| CPU-NPU | 28/28 | 0 | 108 min | combination_12 (S3 전부 NPU), 3회 |
| CPU-GPU | 28/28 | 0 | 108 min | 동일, 3회 |

- **순차 실행** (NPU 완료 후 GPU). 동시 실행 안 함.
- 머신 격리: 수집 중 배경 CPU 작업 없음, run마다 프로세스 재시작.
- 조합 순서 무작위화(seed 고정), anchor 10 run 주기.

---

## 3. 예측 vs 실측 순위 상관 (Spearman)

| 세트 | CPU-NPU | CPU-GPU | 실측 y1 범위 |
|---|---|---|---|
| **S1** (2, vision) | **+0.800** | **+1.000** | 20.8~256.8 (12배) |
| **S3** (3, LLM) | **+0.929** | **+0.976** | 10.5~234.3 (22배) |
| **S8** (8, 층화) | **−0.079** 🔴 | **+0.944** | 22.8~347.7 (15배) |

### S8-NPU 실패의 정체 — **OOD (학습 데이터 부족), 접근 실패 아님**

지시대로 "접근 실패 vs 학습 부족"을 구분했다:

1. **실측은 정상** — S8-NPU 실측 y1이 층에 **+0.938** 단조 증가. 배치가 성능을 가른다.
2. **예측기가 뭉갠다** — 같은 조합들의 **예측** y1은 0.75~0.90에 갇혀 층 상관 **−0.557**
   (오히려 역방향). y2 예측은 전부 1.0 포화.
3. **원인**:
   - 예측기는 **4-뷰 세트로만 학습됨** (`view_counts=[4]`). S8은 **8뷰 → 분포 밖**.
   - **mobilenet_v2를 학습한 적 없음** (`'mobilenet_v2' in cov['models'] = False`).
   → 예측기가 8모델·신규모델 조합을 처음 보는 것이다.

**S8-GPU가 +0.944로 맞은 것**은 GPU에서 배치 간 실측 y1 격차가 워낙 커서(22.8~347.7, 15배)
OOD 예측도 대략의 순위는 보존했기 때문이다. NPU는 격차가 상대적으로 작아(22.8~225.5, 9배)
OOD 노이즈에 순위가 뒤집혔다.

**함의**: 파일럿 예측기(기존 5×4모델 학습)로는 8모델·mobilenet_v2 세트를 예측할 수 없다.
이것이 **바로 전량 수집을 하는 이유다** — S5~S10, mobilenet_v2를 학습에 넣어야 이 OOD가 해소된다.
접근 자체는 4모델 근방(S1/S3, +0.80~+0.98)에서 잘 작동함이 확인됐다.

---

## 4. CPU-NPU vs CPU-GPU 최적 배치 비교

| 세트 | NPU best | GPU best | 다른가 |
|---|---|---|---|
| S1 | combination_4 | combination_4 | 동일 |
| **S3** | combination_8 | combination_12 | **다름** |
| **S8** | combination_20 | combination_14 | **다름** |

- **3모델 이상에서 최적 배치가 플랫폼마다 다르다.** S3: NPU는 combination_8(llama1b CPU),
  GPU는 combination_12(전부 가속기)를 최적으로 본다.
- → **"플랫폼 일반화" 주장의 근거**: 한 플랫폼의 최적이 다른 플랫폼의 최적이 아니므로,
  두 예측기를 따로 두는 설계가 정당하다. **본수집은 양 플랫폼 필수.**

---

## 5. GPU anchor 시계열 — **드리프트 격리로 잡힘** ✅

동일 조합(S3 전부 가속기)을 108분에 걸쳐 3회:

| 시점 | NPU anchor y1 | GPU anchor y1 |
|---|---|---|
| run 0 (3.5분) | 157.74 | 236.81 |
| run 10 (42분) | 158.10 | 235.36 |
| run 20 (80분) | 157.90 | 235.42 |
| **CV** | **0.11%** | **0.35%** |

**본수집 최대 리스크였던 GPU 세션 드리프트가, 머신 격리만으로 완전히 사라졌다.**
(이전 "2시간 후 −21%"는 배경 CPU 작업 탓이었음이 재확인됨.)
anchor 변동(0.35%)은 배치 간 차이(15배)보다 **43배 작다** → 신호가 노이즈에 안 덮인다.

---

## 6. y1 변별력 / y3 마스킹

- **y1 변별력**: S8 층 0→7에서 y1이 NPU 25→225(9배), GPU 23→348(15배). 층 상관 +0.94/+0.96.
  후처리 통일 덕에 배치가 실제로 구분된다. ✅
- **y3 마스킹**: S1(vision-only)에서 `has_generative=False` → 점수식 `y1 − α·y2` (y3 항 제외). ✅

---

## 7. 정찰에서 드러난 실측 특성 (본수집 반영 사항)

### 🔴 CPU llama1b는 180초 창에서 y3가 측정 불가

S3-NPU 조합별 (llama1b 위치 vs y3):

| llama1b 위치 | y3 |
|---|---|
| CPU | 0.00, 0.35, 0.35, 1.22 |
| NPU | 10.40, 12.63, 12.72, 16.63 |

CPU llama1b는 **1회 생성에 198초** 걸려 180초 창에서 0~1건만 완료 → y3가 사실상 0.
이건 측정 오류가 아니라 **"CPU에 LLM 배치는 나쁘다"는 진짜 신호**다. 다만:
- y3 라벨의 **노이즈가 극심**하다 (0~1 샘플).
- **본수집 권고**: CPU-LLM 조합은 y3를 신뢰하지 말 것. 이미 y2가 이 조합을 1.0으로
  올바르게 벌하므로 순위에는 영향 없으나, y3 예측기 학습 시 이 행들의 y3는 노이즈다.
  → **CPU-LLM 조합의 y3도 마스킹 대상에 포함**을 검토할 것 (현재는 세트 단위 마스킹만).

### anchor 선택 교훈
최초 anchor를 llama1b가 CPU인 조합으로 잡아 y3=0 노이즈가 났다. **안정 조합(전부 가속기)으로
교체.** 본수집 anchor도 반드시 전부-가속기 조합으로.

---

## 8. 전량 수집 진입 — 확인된 것 / 추가 필요한 것

### ✅ 파일럿에서 확인
- 실측 파이프라인 정상 (배치가 y1/y2를 합리적으로 가름).
- 4모델 근방에서 예측 접근 유효 (Spearman +0.80~+0.98).
- 플랫폼별 최적 배치가 다름 → 양 플랫폼 수집 정당.
- GPU 드리프트 격리로 해소 (CV 0.35%).
- 층화 축("가속기 배치 수") 유효 (R² 0.89~0.98).
- 무실패 파이프라인, anchor 카나리아 동작.

### 본수집에서 추가로 필요
1. **S5~S10, mobilenet_v2를 학습에 포함** — S8-NPU OOD를 해소하는 유일한 길.
2. **CPU-LLM 조합 y3 마스킹** 검토 (§7) — 세트 단위가 아니라 조합 단위로.
3. **anchor는 전부-가속기 조합** 고정 (§7).
4. 예산: 정본 15세트 전량은 **약 86~97시간** (`PRE_COLLECTION_FINAL.md` §5, 감축안 미승인).

### 판정
**전량 수집 진입 권고.** 접근은 4모델 근방에서 검증됐고, 8모델 OOD는 "더 모아야 하는 이유"이지
"접근이 틀렸다는 증거"가 아니다. GPU 드리프트라는 최대 리스크가 격리로 해소된 것이 특히 크다.

**단, 이 파일럿 데이터로 예측기를 학습·평가하지 않는다** (세트 부족, 대조군 없음, OOD).
정찰 목적으로만 사용.
