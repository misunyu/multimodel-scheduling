# venv & cache
.venv/
__pycache__/
*.pyc
.idea/

# data (large)
data/argoverse_hd/
models/mobilint/*.mxq
models/onnx/*.onnx
models/onnx/*.data

# results (regenerable)
results/
/accv_experiments/

# ORT-CUDA shim: large .so blobs (keep README+lock, ignore libs)
vendor_ort_cu12/nvidia/
vendor_ort_cu12/*.dist-info/
