# Reproduction dependencies for paper/reproduction/*
# Versions pinned to the environment in which all figures/tables were verified
# (repo .venv). Any recent 3.10+ Python with these packages reproduces the
# numbers; exact pins guarantee byte-level plotting parity.
#
# Covers every RUNNABLE generator (11 of 12 items). The one exception,
# fig_failures/ (fig:failures), additionally needs live GPU+NPU hardware plus
# opencv-python, torch, ultralytics and the Mobilint NPU SDK — see
# fig_failures/README.md; it is NOT reproducible in a CPU-only environment and
# ships its exact rendered PDF instead.

pandas==2.3.3
numpy==2.2.6
matplotlib==3.10.9
scipy==1.15.3
