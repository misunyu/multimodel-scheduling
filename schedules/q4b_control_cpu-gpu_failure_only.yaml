# GENERATED by scripts/generate_schedules.py on 2026-07-29 -- do not hand-edit.
# scenario      : q4b_control
# platform      : cpu-gpu
# working set   : ['yolo11s', 'resnet50', 'mobilenet_v2']  (foreground vision = ['yolo11s', 'resnet50', 'mobilenet_v2'], background generative = [])
# candidate order: READ FROM the ranking below (ranks 1..5); never hand-written.
# ranking file  : rankings/ranking_Q4b_control_v3_cpu-gpu.json
# ranking sha256: 5cca6b7eacd20b2a3942e6cb2b3f5ba943ec9914bc026f7a367553fa705a25a0
# alpha / beta  : 0.3 / 1.0
# lambda        : stable 25 fps/view, burst 45 fps/view (generative 2 req/s); uniform L_SLO 15 ms
# active background: none
# failure phase : allcpu  (combination_burst = all models on CPU)
#
# The Q6 ablation corners and the Adaptive baseline use THIS file unchanged and are
# distinguished by executor mode/trigger flags only.
# STATIC-ONLY: only the failure phase; Static must start here.

combination_burst:
    m1:
        display: view1
        execution: cpu
        infps: 45
        slo_ms: 15
        model: yolo11s
    m2:
        display: view2
        execution: cpu
        infps: 45
        slo_ms: 15
        model: resnet50
    m3:
        display: view3
        execution: cpu
        infps: 45
        slo_ms: 15
        model: mobilenet_v2
