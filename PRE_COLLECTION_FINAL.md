# 대규모 재수집 직전 최종 관문 보고서

- 날짜: 2026-07-15
- 코드: `33676fe` (지표 정의 수정) 기준
- 측정: GPU 드리프트 18회 + NPU 드리프트 10회 + rate 스윕 20회 + 배치 스윕 12회 + A/B 3회 = **63 runs**

---

## 0. 요약 — **진입 가능. 남은 승인 2건.**

| PART | 결과 |
|---|---|
| **1. 지표 정의** | ✅ y1=달성처리량 / 워밍업 20초 / y3 마스킹 — 전부 적용·검증 |
| **2. "GPU 드리프트"** | 🔴 **오진이었다. GPU도 NPU도 드리프트하지 않는다.** 진범은 **동시 실행된 CPU 작업** |
| **3. rate factor** | ✅ 확정. **세트 크기별로 달라야 한다** |
| **3. y1 변별력** | ✅ 배치를 8.7배 차이로 구분 (23 → 203) |
| **3. duration** | ✅ **180초 유지** (120초로 줄이면 33% 절감 가능, ±5% 기준) |

**수집 조건 (필수)**
1. **머신을 완전히 격리할 것** — 배경 CPU 작업 하나가 y1을 −73% 왜곡한다 (§2)
2. **rate factor를 세트 크기별로 차등** — 8모델에 4×를 쓰면 y2가 죽는다 (§3.1)
3. 예산 승인 (§5) — 전수는 **124시간**

---
---

# PART 1 — 지표 정의 수정 ✅

## 1.1 y1 = 달성 처리량 (완료건수 / 측정창)

`view_handlers.py`의 `avg_fps = 1000/avg_infer_time` 을 폐기하고 **완료 건수 / 실측 창**으로 교체.

**실측 검증** (case_A, NPU, 60초):

| 모델 | 구 y1 (`1000/추론시간`) | **신 y1** | 완료건수/창 | 일치 |
|---|---|---|---|---|
| resnet50 | 315.4 | **95.77** | 95.77 | ✅ |
| yolo11s | 233.4 | **41.80** | 41.80 | ✅ |

수렴 실험에서 관측했던 "보고 315.4 vs 실제 100.2"의 **7배 허수가 사라졌다.**

## 1.2 워밍업 20초 + y1/y2 정의 통일 ✅

- `WARMUP_SECONDS = 20` (env로 override 가능). `unified_viewer.py`
- 측정창 시작(`_measure_t0`)을 워밍업 종료 시점에 찍고, 종료 시점(`_measure_t_end`)에 얼린다.
- **y2의 `offered`가 y1과 동일한 실측 창을 나눈다** → 두 타깃이 같은 e2e 정의를 공유한다.
  (이전에는 y1=forward pass, y2=e2e 로 서로 다른 것을 재고 있었다)
- 단계별 분해(preprocess/inference/postprocess/visualize)는 계속 기록된다.

## 1.3 y3 마스킹 ✅

`combo_has_generative()` — `reg.kind_of()` 기반, **하드코딩 없음**.

| 세트 | has_generative | **로드된 부스터** | 점수 |
|---|---|---|---|
| case_A (resnet50, yolo11s) | False | **`['y1.json', 'y2.json']`** | 0.550 |
| case_D (llama1b, qwen2_vl, resnet50, yolo11s) | True | `['y1.json','y2.json','y3.json']` | 1.035 |

- vision-only에서 **y3 부스터를 로드조차 하지 않는다** (호출 후 버리는 게 아니다).
- 점수식: `S = y1 − α·y2` (+ `β·y3` 는 생성 모델이 있을 때만).
- 예전이라면 유령 가점으로 0.550 대신 **0.997**이 나왔다.
- **학습**: `train_targets()`가 y3 학습에서 vision-only 행을 제외한다 (`views.sum.view.is_llm > 0` 마스크).

> ⚠️ **점수 스케일이 세트 유형에 따라 달라진다. 세트 간 점수 직접 비교 금지.**
> 점수는 **같은 working set 내 조합 순위용**이다. 코드 주석에 명시했다.

## 1.4 가시화 OFF 수집 모드 ✅
`DISABLE_VISUALIZATION=1` → 모든 뷰 headless, 실행·측정 불변.
ON/OFF 차이는 수렴 리포트에서 이미 측정됨 (y1 ±1.2~2.4%, 반복 CV와 같은 자릿수).

## 1.5 스케줄 네이밍 ✅
재수집 스케줄은 **`combination_N`** 규약을 따른다 (`_iter_combos_from_schedule`가 그 접두사만 인식).

---
---

# PART 2 — "GPU 세션 드리프트"는 오진이었다 🔴

## 2.1 GPU는 결백하다 (18회 연속, 45분)

| GPU 지표 | 전반 | 후반 | 변화 |
|---|---|---|---|
| 온도 | 56.0°C | 56.3°C | +0.6% |
| **SM 클럭** | **2529 MHz** | **2529 MHz** | **−0.0%** |
| 전력 | 189.0 W | 190.2 W | +0.6% |
| GPU 메모리 | 안정 | 안정 | 누수 없음 |
| **스로틀 사유** | **전 구간 `0x0`** | | **스로틀 전혀 없음** |

**새 y1은 18회 내내 안정**: 331.30 → 330.01 (**편차 ±0.5%**).

## 2.2 NPU도 드리프트하지 않는다 (10회 연속, 25분)

y1 = 200.38 → 203.36 (**±1%**), y2 = 0.556~0.571, y3 = 16.45~17.31.

## 2.3 진범: **동시 실행된 CPU 작업** — A/B로 확정

드리프트 실험 중 run 11에서 **시스템이 갑자기 빨라졌다** (llama1b 1380→1180ms).
그 시점에 바뀐 것은 GPU가 아니라 **내가 트레이스 분석(수십 MB numpy 파싱)을 멈춘 것**이었다.

**결정적 A/B** (동일 조합 case_E, GPU, 120초):

| 조건 | y1 | y2 | y3 | llama1b | qwen2_vl | yolo11s |
|---|---|---|---|---|---|---|
| **유휴** | **330.80** | **0.3347** | **54.46** | 1,455 ms | 726 ms | 18.21 ms |
| **CPU 부하 8코어** | **90.31** | **0.7574** | **24.02** | 4,034 ms | 1,915 ms | 43.86 ms |
| 변화 | **−72.7%** | **+126.3%** | **−55.9%** | +177% | +164% | +141% |

**배경 CPU 작업 하나가 y1을 −73%, y2를 +126% 왜곡한다.**

원리는 단순하다: **전처리·후처리·LLM 샘플링·KV캐시가 전부 CPU에서 돈다.** 모델을 NPU에
올리든 GPU에 올리든 CPU는 항상 쓰인다. CPU가 경합하면 **모든 디바이스의 측정이 동시에 오염된다.**

보조 증거 — CPU는 실제로 스로틀링한다:
```
Intel Core Ultra 9 285K (최대 5100 MHz)
부하 중 평균 2883 MHz  ← 최대의 56%
package_throttle_count: 21,426회
```

## 2.4 판정 및 보정 설계

| 질문 | 답 |
|---|---|
| 드리프트 크기 | 하드웨어 기인 드리프트는 **없다** (GPU ±0.5%, NPU ±1%) |
| 원인 | **동시 CPU 작업** (하드웨어 아님) |
| 회복 조건 | **부하를 없애면 즉시 회복**. 재시작·쿨다운 불필요 |
| NPU도 드리프트? | **아니오** |

**보정 설계 (원래 계획보다 단순해졌다):**

1. **🔴 머신 격리 (필수)** — 수집 중 다른 CPU 작업 금지. 분석·빌드·IDE 인덱싱·브라우저 포함.
   → 수집 스크립트는 결과 파일만 쓰고, **분석은 수집이 끝난 뒤에** 한다.
2. **Anchor 재측정 (권장)** — N개 run마다 고정 기준 조합 1개를 재측정.
   드리프트 방지책이 아니라 **격리가 깨졌는지 탐지하는 카나리아**로 쓴다. 비용: 세션당 1 run.
3. **조합 순서 무작위화 (권장)** — 만약 격리가 깨져도 특정 조합에 몰리지 않게 분산.
4. **쿨다운·세션 분할: 불필요** — 하드웨어 드리프트가 없으므로.

**예산 추가 비용: anchor 1 run / 세션 ≈ 무시할 수준.**

---
---

# PART 3 — 재확인 스윕

## 3.1 rate factor — **세트 크기별로 달라야 한다** 🔴

placement 고정(가속기 우선), rate factor 5단계, 120초.

### 8-모델 세트 (case_E)

| rate | **CPU-NPU** y1 / y2 | **CPU-GPU** y1 / y2 |
|---|---|---|
| **1.0×** | 204.6 / **0.561** | 333.8 / **0.291** |
| **1.5×** | 177.4 / **0.775** | 347.8 / **0.535** |
| **2.0×** | 138.1 / **0.889** | 334.6 / **0.801** |
| 3.0× | 107.0 / 0.964 🔴 | 324.6 / 0.935 |
| 4.0× | 95.2 / **0.998** 🔴 | 319.1 / 0.945 |

→ **NPU는 3.0×에서 이미 포화**(0.964 ≥ 0.95). GPU는 4.0×에서 0.945로 아슬아슬.
→ **8-모델 유효 구간: 1.0× ~ 2.0×**

### 2-모델 세트 (case_A)

| rate | **CPU-NPU** y1 / y2 | **CPU-GPU** y1 / y2 |
|---|---|---|
| 1.0× | 137.3 / **0.235** | 137.3 / **0.233** |
| 1.5× | 222.6 / 0.211 | 222.5 / 0.206 |
| 2.0× | 237.9 / 0.325 | 238.4 / 0.316 |
| **3.0×** | 245.0 / **0.848** | 260.0 / **0.837** |
| **4.0×** | 243.7 / **0.911** | 261.9 / **0.910** |

→ 저부하에서 y2가 0.21~0.33으로 낮다. **3.0× 이상에서야 신호가 생긴다.**
→ **2-모델 유효 구간: 2.0× ~ 4.0×**

### 🔴 저부하에서 배치가 전혀 구분되지 않는다 (중대)

**case_A의 CPU-NPU와 CPU-GPU 값이 사실상 동일하다:**
- 1.0×: y1 **137.3 vs 137.3**, y2 **0.235 vs 0.233**
- 2.0×: y1 237.9 vs 238.4, y2 0.325 vs 0.316

두 가속기 모두 공급량을 다 처리하므로 **배치가 성능에 아무 차이를 만들지 않는다.**
이 구간의 데이터는 **예측기에 아무것도 가르치지 못한다** (라벨이 같으니 특징만 다른 노이즈다).
소형 세트의 저 rate 구간은 **예산 낭비**다.

### 3~5 모델 구간 — **측정 완료** (18 runs, 격리 상태)

placement 고정(가속기 우선), rate 1×/2×/3×, 120초. 세트 B(llama1b+resnet50+yolo11s),
C(qwen2_vl+resnet50+yolo11s), D(4모델).

| 세트 | **CPU-NPU** y2 (1×/2×/3×) | **CPU-GPU** y2 (1×/2×/3×) |
|---|---|---|
| **B** (3, LLM) | 0.43 / 0.63 / 0.81 | 0.19 / 0.30 / 0.64 |
| **C** (3, VLM) | 0.58 / 0.81 / **0.95** 🔴 | 0.18 / 0.56 / 0.90 |
| **D** (4) | 0.64 / 0.83 / 0.90 | 0.17 / 0.50 / 0.73 |

- **1×/2×/3× 모두 y2에서 변별력이 있다** (단계 간 간격이 충분히 벌어진다).
- **VLM 세트(C)의 NPU만 3×에서 포화**(0.95). LLM 세트(B)와 4모델(D)은 3×도 유효.
- ⚠️ **2모델과 결정적으로 다르다: 배치가 1×에서도 구분된다.** 같은 세트/rate에서 NPU y1과
  GPU y1이:

  | 세트 | 1× | 2× | 3× |
  |---|---|---|---|
  | B | 16.0% | 33.2% | 44.6% |
  | C | 16.6% | 30.8% | 42.9% |
  | D | **31.7%** | **58.4%** | **68.1%** |

  가속기가 공급을 다 흡수하지 못하므로(2모델과 달리) **배치가 1×부터 성능을 가른다.**
  → 3~5모델은 저 rate 구간을 버릴 이유가 없다.

### 권장 (최종, 전 구간 측정 완료)

| 세트 크기 | rate factors | 근거 |
|---|---|---|
| **≥6 모델** | **1.0×, 1.5×, 2.0×** | 3.0×부터 y2 포화 (NPU 0.964) |
| **3~5 모델** | **1.0×, 2.0×, 3.0×** | ✅ 3단계 모두 y2 변별력; 배치는 1×부터 구분됨 (VLM-NPU만 3× 경계) |
| **≤2 모델** | **2.0×, 3.0×, 4.0×** | 저 rate는 배치 변별력이 0 (NPU=GPU) |

## 3.2 새 y1의 배치 변별력 — **매우 좋다** ✅

8-모델 세트, NPU에 올린 모델 수를 1→8로 바꿔가며 (rate 1.0×):

| NPU에 올린 모델 수 | **y1** | **y2** | y3 |
|---|---|---|---|
| 1 | **23.25** | 1.000 | 6.80 |
| 3 | 50.58 | 0.832 | 7.29 |
| 4 | 70.34 | 0.766 | 7.64 |
| 5 | 105.89 | 0.735 | 7.65 |
| 6 | 191.65 | 0.698 | 7.78 |
| **8** | **202.61** | **0.567** | 17.55 |

**y1이 23 → 203 으로 8.7배 벌어지며 단조 증가한다.** y2도 1.00 → 0.57 로 단조 감소.
**배치가 성능을 실제로 가른다** — 예측기가 배울 신호가 충분하다.
(후처리 통일 전이었다면 e2e가 CPU 후처리에 지배되어 이 신호가 묻혔을 것이다.)

### rate 4.0×에서는 y2가 죽는다

| NPU 모델 수 | y1 | **y2** |
|---|---|---|
| 1 | 21.81 | **1.0000** |
| 3 | 53.78 | **1.0000** |
| 4 | 77.66 | 0.9999 |
| 5 | 108.60 | **1.0000** |
| 6 | 172.77 | 0.9999 |
| 8 | 96.60 | 0.9972 |

**y2가 전부 1.0에 붙어 변별력이 완전히 사라진다.** y1은 여전히 구분하지만(22~173),
순서가 뒤집힌다(8개 배치가 6개보다 나쁨 — 과부하에서 가속기가 병목이 됨).
→ **8-모델 세트에 4.0×를 쓰면 y2 라벨이 상수가 되어 학습에 기여하지 못한다.** §3.1의 결론과 일치.

## 3.3 duration — **180초 유지** ✅

새 y1(완료건수 기반) 수렴, 워밍업 20초 제외:

| run | y1 ±2% | y2 ±2% | y3 ±2% |
|---|---|---|---|
| npu case_E ×3 | 75~110s | 75~110s | 45~100s |
| gpu case_E ×3 | 10~30s | **110~120s** | 55~90s |

- **새 y1은 훨씬 빨리 수렴한다** (GPU 10~30초; 구 정의는 40~220초였다).
- **y2가 여전히 하한을 결정한다: 최대 120초.**
- → **180초 유지** (여유 50%). **120초로 줄이면 ±5% 기준으로는 충분하며 예산 33% 절감** (§5 선택지).

워밍업 20초: llama1b 첫 추론 13.4초를 덮는다. ✅

## 3.4 y3 마스킹 실행 확인 ✅
vision-only 세트(case_A)로 예측·스케줄 생성·실행 전부 정상. y3 부스터 미로드, 점수에 β항 없음.

## 3.5 anchor 안정성 ✅
드리프트 실험 자체가 anchor 반복이다: GPU 18회 y1 CV **0.4%**, NPU 10회 CV **0.9%**.
**배치 간 차이(8.7배)에 비해 anchor 변동은 무시할 수준이다** → 신호가 노이즈에 덮이지 않는다.

---
---

# 5. 재수집 예산

## 5.1 실측 기반 단가

```
run 1회 = 워밍업 20s + duration 180s + 로드/정리 ~7s = 207초 = 3.45분
  (실측: duration 120초 run의 wall time 147초 → 오버헤드 7초, 모델 로드는 워밍업과 겹침)
```

## 5.2 조합 수

| 세트 크기 | 배치 조합 수 (플랫폼당) |
|---|---|
| 2 모델 | 4 |
| 4 모델 | 8 |
| **8 모델** | **128** |

## 5.3 전수 측정 (⚠️ 비현실적)

15개 working set의 정확한 구성이 필요하다. **13개가 4-모델, 2개가 8-모델**이라 가정하면:

```
(13 × 8  +  2 × 128) × 3 rates × 2 platforms = 2,160 runs
2,160 × 207초 = 124 시간
```

**8-모델 세트 2개가 전체의 71%를 잡아먹는다** (256 / 360 조합).

## 5.4 감축안 (제안 — 승인 없이 적용하지 않음)

| # | 방안 | 절감 | **무엇을 잃는가** |
|---|---|---|---|
| **A** | 8-모델 세트 **층화 샘플링** (128 → 32, "NPU에 올린 모델 수" 0~8 층별 균등) | 124h → **58h** | 8-모델 배치공간의 75%를 안 본다. §3.2에서 y1이 NPU 개수에 **단조**임을 확인했으므로 층별 샘플링이 형태를 잡아낼 가능성은 높지만, **비단조 구간(과부하 시 순서 역전)을 놓칠 수 있다** |
| **B** | rate 3단계 → **2단계** | ×0.67 | rate 축의 해상도를 잃는다. §3.1에서 유효 구간이 이미 좁으므로(8모델 1.0~2.0×) **2단계면 양 끝만 보게 된다** |
| **C** | duration 180 → **120초** | ×0.71 | y2가 ±2% 대신 **±5%** 수렴에 머문다 (§3.3). 라벨 노이즈가 커진다 |
| **D** | 소형 세트(≤2모델)의 **저 rate 제거** | 소폭 | §3.1에서 **정보가 0**임을 확인했으므로 **잃는 것이 없다** ✅ |

**조합별 예산:**

| 시나리오 | 시간 |
|---|---|
| 전수 | **124 h** |
| A (층화만) | **58 h** |
| **A + D** | **~55 h** |
| **A + B + D** | **~37 h** |
| **A + B + C + D** | **~26 h** |
| A + C + D | ~39 h |

**권고: A + D (55시간)** 부터 검토. B(rate 축소)와 C(duration 단축)는 **라벨 품질을 직접 깎으므로**
예산이 정말 안 맞을 때만 쓸 것. D는 손실 없이 공짜다.

> 🔴 **15개 working set의 정확한 구성(모델 목록)이 필요하다.** 위 계산은 "13×4모델 + 2×8모델"
> 가정이다. S1/S2/S9/S10이 vision-only라 했으므로 실제 구성에 따라 숫자가 달라진다.

---

# 6. 진입 체크리스트

| 항목 | 상태 |
|---|---|
| y1 = 달성 처리량 | ✅ 적용·검증 |
| 워밍업 20초 | ✅ |
| y1/y2 같은 e2e 정의 | ✅ |
| y3 마스킹 (예측·학습·점수) | ✅ |
| 가시화 OFF 수집 모드 | ✅ |
| `combination_N` 네이밍 | ✅ |
| GPU/NPU 하드웨어 드리프트 | ✅ **없음** |
| **머신 격리** | 🔴 **수집 중 필수** (배경 CPU 작업 → y1 −73%) |
| rate factor | ✅ 확정 (세트 크기별 차등) |
| **3~5 모델 rate 확인** | ✅ **측정 완료** (18 runs) — 1×/2×/3× 확정 |
| duration 180초 | ✅ |
| y1 배치 변별력 | ✅ 8.7배 |
| **15개 세트 구성** | 🔴 **필요** |
| **예산 승인** | 🔴 **필요** (전수 124h / 권고안 55h) |

**남은 승인 사항 2개: (1) 15개 working set 구성 제공, (2) 예산 감축안 선택.**
(3~5모델 rate 확인은 완료되어 블로커에서 제외됨.)
