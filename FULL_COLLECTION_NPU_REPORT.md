# Phase A — CPU-NPU 본수집 · deploy_cpu_npu 학습·검증 보고서

- 날짜: 2026-07-15 ~ 07-17
- 수집: 540 조합 (15 working set × 층화/전수 × rate 3단계), **0 실패**, 약 32시간
- 산출: `deploy_cpu_npu_{y1,y2,y3}.json` + `_features.json` + `_coverage.json`
- 판정: **✅ Phase B(CPU-GPU) 진행 권고**

---

## 0. 요약

| 항목 | 결과 |
|---|---|
| 수집 | 540/540 조합, 0 실패 |
| anchor 드리프트 | 30회 **CV 0.39%** (격리 완벽) |
| **S8-NPU OOD 해소** | 파일럿 −0.079 → **본수집 +0.991** (out-of-fold) |
| 전체 out-of-fold Spearman | **+0.954** (15세트, 45개 rate 그룹) |
| GUI 검증 | 15세트 표시, 전 크기 **OOD 경고 0건** |

---

## 1. 예산·층화 (공통-0/1)

정본 `working_sets.yaml` 기준 CPU-NPU 단독 **540 조합** (감축안 A3: 자유배치 ≥5 세트 전부 층화).

| 세트 | 모델수 | 자유배치 | 전조합 | 수집 | rate |
|---|---|---|---|---|---|
| base1~5 | 4 | 3 | 8 | 8 | 1/2/3× |
| S1 | 2 | 2 | 4 | 4 | 2/3/4× |
| S2, S3 | 3 | 3 | 8 | 8 | 1/2/3× |
| S4 | 3 | 2 | 4 | 4 | 1/2/3× |
| S5 | 5 | 4 | 16 | 16 | 1/2/3× |
| **S6** | 6 | 5 | 32 | **16 (층화)** | 1/1.5/2× |
| **S7** | 7 | 6 | 64 | **20 (층화)** | 1/1.5/2× |
| **S8** | 8 | 7 | 128 | **24 (층화)** | 1/1.5/2× |
| **S9** | 5 | 5 | 32 | **16 (층화)** | 1/2/3× |
| **S10** | 7 | 7 | 128 | **24 (층화)** | 1/1.5/2× |

층화 축 = "가속기 배치 모델 수" (기존 데이터 R² y1 0.89 / y2 0.98). 5개 층화 세트 모두
**전 층(0..f) 커버 + 극단 포함 + 중간층 ≥2**.

## 2. 스케줄 (공통-2)
`combination_1..540`, CPU-NPU/CPU-GPU 양쪽 생성 (배치 정렬). 검증: 파서 540/540,
combination_unknown 0, qwen2_vl CPU위반 0, **플랫폼 배치정렬 0 불일치**.

## 3. y3 조합단위 마스킹 (공통-3)
`featurize_window`가 조합별 `y3_valid`(LLM/VLM이 실제 가속기에 배치)를 기록.
학습에서 **168/540 행 마스킹**(CPU-LLM + vision-only), y3는 372행으로만 학습.
- 파일럿 발견 반영: CPU llama1b는 198s/생성이라 180s 창에서 0~1건 완료 → y3 노이즈.

## 4. held-out (공통-4)
`out_of_fold_predictions()`: 3-fold, 각 조합을 그 조합을 학습에 안 쓴 모델로 예측.
검증 Spearman은 전부 이 out-of-fold 예측으로 계산 (과적합 차단).

---

## 5. 수집 결과 (A.1)

| 그룹 | 조합 | 실패 | 소요 |
|---|---|---|---|
| S8 (체크포인트 대상) | 72 | 0 | 269분 |
| S10 | 72 | 0 | 268분 |
| base1~5 | 120 | 0 | 440분 |
| S7 | 60 | 0 | 222분 |
| S9 | 48 | 0 | 178분 |
| S6 | 48 | 0 | 179분 |
| S5 | 48 | 0 | 179분 |
| S1~S4 | 76 | 0 | ~280분 |
| **합계** | **540** | **0** | **~32h** |

- 격리: 다른 CPU/GPU 부하 없음, run마다 프로세스 재시작, vision 워커 torch=1.
- **anchor(전부-NPU S3) 30회 CV 0.39%** — 배치 간 차이(8.7배) 대비 43배 작아 신호를 안 덮음.

## 6. 체크포인트 (A.2) — OOD 조기 확인

S8+S10+base1~5 (264조합, 15.6h) 시점에 정지, 별도 경로에 임시 학습:

| 세트 크기 | out-of-fold Spearman |
|---|---|
| 4모델 | +0.929 |
| 7모델 | +0.992 |
| **8모델 (S8)** | **+0.989** |

**S8-NPU: 파일럿 −0.079 → 체크포인트 +0.989.** OOD 해소 확인 후 나머지 수집 계속.
(최종 아티팩트는 이 시점에 건드리지 않음 — 임시 학습은 checkpoint 전용.)

## 7. 최종 학습 (A.3)

`deploy_cpu_npu_{y1,y2,y3}.json` 학습 (기존 아티팩트는 `pre_recollect_backup/`로 백업):
- 540행, 37 feature, **view_counts 2~8**, 9모델 (mobilenet_v2 포함)
- CV MAE: y1 0.039, y2 0.070, y3 0.030
- y3: 168행 마스킹 후 372행 학습
- coverage: **15 model_sets, view_counts [2,3,4,5,6,7,8]**

## 8. 검증 (A.4) — out-of-fold, 학습에 안 쓴 예측

| 세트 | 모델수 | 평균 Spearman | 범위 |
|---|---|---|---|
| S1 | 2 | +1.000 | — |
| S2/S3 | 3 | +0.976 | — |
| S4 | 3 | +0.800 | — |
| base1~5 | 4 | +0.857~+0.984 | — |
| S5 | 5 | +0.981 | — |
| S6 | 6 | +0.988 | — |
| S7 | 7 | +0.981 | — |
| S9 | 5 | +0.972 | — |
| **S8** | 8 | **+0.991** | [+0.99,+0.99] |
| S10 | 7 | +0.990 | — |

- **전체 45개 (세트,rate) 그룹 평균 +0.954.** 15세트 모두 +0.80 이상.
- **S8-NPU: 파일럿 −0.079 → +0.991** (이번 수집의 성패 지표, 회복).
- GUI: CPU-NPU 선택 시 **15세트 표시, 전 크기 OOD 경고 0건**.
- y3 마스킹: vision-only 세트(S1, base)는 점수식에서 y3 항 제외.

---

## 9. Phase A 판정

| 질문 | 답 |
|---|---|
| deploy_cpu_npu가 배치 순위를 맞히는가? | ✅ out-of-fold 평균 +0.954 |
| **S8-NPU OOD 해소?** | ✅ **−0.079 → +0.991** |
| GUI 15세트, OOD 경고 없음? | ✅ |
| y3 조합마스킹 동작? | ✅ 168행 제외 |
| 격리로 드리프트 방지? | ✅ anchor CV 0.39% |

**→ Phase B(CPU-GPU) 진행 권고.** Phase B는 이 Phase에서 확정한 레시피
(feature 37개, y1=완료건수/창, y3 조합마스킹, (models,workload,rate) 정규화,
층화 축·목표, out-of-fold)를 **그대로** 사용한다. CPU-GPU 스케줄은 공통-2에서 이미
배치 정렬 상태로 생성됨.

**사용자 확인을 받아 Phase B로.**
