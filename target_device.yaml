# target_device.yaml
# Configuration for target system devices

# Target system specification
devices:
  # CPU specification
  cpu:
    count: 1

  # GPU specification (NVIDIA CUDA)
  gpu:
    count: 1

  # NPU specification (Mobilint Aries)
  npu:
    count: 1
    ids: [0]
