# target_device.yaml
# Configuration for target system devices

# Target system specification
devices:
  # CPU specification
  cpu:
    count: 1
    
  # NPU specifications
  npu:
    count: 2
    ids: [0, 1]  # NPU numbers are 0 and 1