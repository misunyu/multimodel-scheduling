# 점수식 계수 (w1=1.0, β=0.5, α=0.3) 재검토 조사

> **성격**: 조사(read-only). 계수·코드·데이터 변경 없음. 수치는 본수집 540창 × 2플랫폼과
> 15세트 예측(GUI Predict Best 경로, rate 1.0 전 배치)에서 실측.
> **점수식(현재 코드)**: `S = 1.0·y1 − 0.3·y2 (+ 0.5·y3, 생성 세트만)`.

## 전제 정정 (중요)
작업 배경은 y2 스케일링을 `min(1, y2/0.5)`로 기술했으나, **실제 현재 코드는 y2를
`(models,workload,rate)` 그룹 min-max 정규화**(직전 작업 `Y2_NORMALIZATION.md`, y2 booster 재학습,
mtime 07-20 15:29)로 [0,1]화한다. 두 방식 다 "세 항 [0,1]"이라 계수 분석 결론은 동일하지만,
아래 분석은 **실제 코드 상태(min-max)** 기준이다.

## 0. 결론 요약

| 항목 | NPU | GPU |
|---|---|---|
| 항 기여 (평균\|·\|) | y1 0.47 / β·y3 0.33 / α·y2 0.20 | y1 0.43 / β·y3 0.23 / α·y2 0.19 |
| β 민감도 (top-1 변동/15) | **중간** (β=0→7, β=1→3) | **0** (전 스윕 무변동) |
| α 민감도 (top-1 변동/15) | 약함 (최대 4) | **0** |
| vision-only α 영향 | **0 변동** (α 0~0.6) | **0 변동** |
| 계수가 측정 최적을 바꾸는 그룹 | 24/45 (53%) | 8/45 (18%) |

**핵심: 계수는 NPU에서만 결정에 영향을 주고, GPU에선 사실상 무의미하다.** 항 기여 순위
(y1 > β·y3 > α·y2)는 의도한 우선순위와 일치하며, 어느 항도 압도/무시되지 않는다.

---

## 1. 세 항의 기여도 분포 (세트 유형별, 양 플랫폼)

예측 정규화값에 계수를 곱한 항의 크기:

| 항 | NPU 전체 | GPU 전체 |
|---|---|---|
| `y1` (w1=1.0) | mean 0.471, std 0.236, [0.065, 1.000] | mean 0.427, std 0.219, [0.053, 1.000] |
| `0.5·y3` (생성만) | mean 0.332, std 0.106, [0.191, 0.500] | mean 0.226, std 0.127, [0.061, 0.500] |
| `0.3·y2` | mean 0.202, std 0.078, [0.000, 0.300] | mean 0.190, std 0.076, [0.000, 0.300] |

- **순위: y1 (최대) > β·y3 > α·y2.** 의도("vision 처리량 최우선 > LM QoS 보조 > vision deadline
  페널티")와 정확히 일치. 어느 항도 0에 눌리거나 다른 항을 압도하지 않는다.
- **vision-only 세트**(S1/S2/S9/S10, y3 항 없음 → y1 vs 0.3·y2 2파전):
  - NPU: y1 mean 0.356 vs 0.3·y2 mean 0.229 → y2 상대 비중이 커 보이지만, **top-1은 안 바뀜**(§4).
  - GPU: y1 mean 0.411 vs 0.3·y2 0.198.

## 2. α/β 순위 민감도 (top-1 변동 세트 수 / 15)

기존 예측값으로 스코어링만 재계산. baseline = (α=0.3, β=0.5).

**β 스윕 (α=0.3 고정)**
| β | NPU 변동 | GPU 변동 |
|---|---|---|
| 0.0 | **7** (base1-5, S6, S7) | 0 |
| 0.25 | 5 | 0 |
| 0.5 | 0 (기준) | 0 |
| 0.75 | 2 (S3, S5) | 0 |
| 1.0 | 3 (S3, base3, S5) | 0 |

**α 스윕 (β=0.5 고정)**
| α | NPU 변동 | GPU 변동 |
|---|---|---|
| 0.0 | 4 (base1, base2, S7, S8) | 0 |
| 0.15 | 1 (base2) | 0 |
| 0.3 | 0 (기준) | 0 |
| 0.45 | 3 (S3, base3, S5) | 0 |
| 0.6 | 4 | 0 |

- **NPU: β가 α보다 민감** (β 제거 시 7세트, α 제거 시 4세트). β는 LM 유무를 가르므로 생성
  세트를 크게 흔든다.
- **GPU: α·β 모두 완전 둔감** — 어떤 값에서도 top-1 불변. GPU는 배치 결정이 계수에 강건.
- **부호 유지**: α는 스윕 전 구간 ≥0 → `−α·y2`는 항상 miss 큰 배치를 페널티. 방향 뒤집힘 없음
  (α=0은 y2 무시일 뿐 역전 아님). 스윕에서 이상 징후 없음.

## 3. β=0.5의 의미 — LM QoS 가중치 · B.5 의존성

**β별 top-1의 llama 배치** (생성 세트; acc=가속기, S4는 llama 없음):

| 세트 | β=0.0 | 0.25 | 0.5 | 0.75 | 1.0 |
|---|---|---|---|---|---|
| **S3 (NPU)** | cpu | cpu | **cpu** | **acc** | acc |
| base1-5 (NPU) | cpu/일부acc | 대부분 cpu→acc | **acc** | acc | acc |
| S5,S8 (NPU) | acc | acc | acc | acc | acc |
| **모든 세트 (GPU)** | acc | acc | **acc** | acc | acc |

- **NPU에서만 β가 llama 배치를 가른다.** β=0.5에서 **S3만 llama를 CPU에** 두고(나머지는 이미
  가속기), **β≥0.75면 S3도 가속기로 이동**.
- **GPU는 β와 무관하게 항상 llama를 가속기에** — GPU가 LLM을 충분히 빨라 throughput·토큰 둘 다
  가속기가 유리, 트레이드오프 자체가 없음.
- **B.5 재검토**: B.5의 "NPU는 llama를 CPU에"는 (a) `compare_platforms.py`의 **raw fps 스코어링**
  산물(`LM_DEADLINE_AUDIT.md`)이고, (b) 현재 정규화 스코어링에선 **S3만** 해당하며 이마저
  **β 의존적**(β≥0.75에서 해소). 즉 β=0.5는 "S3에서 vision 처리량을 위해 llama를 CPU로 밀어냄"을
  의미하고, LM QoS를 우선하려면 β를 올리면 된다.

## 4. vision-only 세트에서 α의 역할

- S1/S2/S9/S10에서 α를 0~0.6으로 스윕해도 **top-1 변동 0** (양 플랫폼).
- 이유: vision-only에서 **처리량 최선 배치 = deadline 최선 배치**가 대체로 일치(전 모델 가속기).
  y1이 순위를 지배하고 α·y2가 이를 뒤집지 못한다.
- → **α는 vision-only에서 top-1에 사실상 무영향.** α=0.3은 결정에 거의 기여하지 않는 약한 레버.

## 5. 실측 최적과의 관계 (목적함수 명시, 순환논리 회피)

계수엔 "정답"이 없으므로, **계수-무관 목적함수(측정 throughput 최대)**를 기준으로 현재 계수의
종합-최적이 얼마나 벗어나는지 측정 (측정 540창):

- **목적함수 A (계수 무관)**: 측정 y1(raw fps) 최대 배치.
- **목적함수 B (현재 계수)**: 측정 정규화값에 (1.0, 0.3, 0.5) 적용한 종합 최대.
- **A==B 비율**: NPU **21/45 (47%)**, GPU **37/45 (82%)**.
  → NPU는 **53% 그룹에서 계수가 선택을 throughput-최적에서 이동**시킨다(계수가 실질 작동).
    GPU는 82%가 일치(계수 영향 미미) — §2의 GPU 둔감성과 정합.
- **예측 정확도(계수와 별개)**: 예측 top-1이 측정을 얼마나 맞히는지는 out-of-fold Spearman으로
  이미 검증됨 (NPU +0.954, GPU +0.970; `FULL_COLLECTION_*_REPORT.md`). 계수 선택과 예측 정확도는
  분리된 문제다. (순환 방지: "종합-최적"을 현재 계수로 정의해 예측과 대조하지 않았다.)

---

## 6. 권고 (계수는 정책 파라미터 — 변경은 후속 작업)

**현재 (1.0, 0.5, 0.3)은 대체로 적절하다.** 근거:
- 세 항 기여가 균형적(y1 0.47 / β·y3 0.33 / α·y2 0.20), 순위가 의도와 일치, 압도/무시 없음.
- GPU는 계수에 완전 둔감 → GPU 결정엔 튜닝 실익 0.

**레버별 성격 (정책 결정 근거)**:
- **α (vision deadline 페널티) — 약한 레버**: vision-only top-1 0 변동, 전체도 저민감.
  현재 0.3 유지 무방. 낮춰도(α→0.15) 결정 거의 불변 → 튜닝 실익 적음. α는 "동점일 때 miss 적은
  쪽" 정도의 미세 조정 역할.
- **β (LM QoS) — NPU의 결정적 레버**: β=0.5는 **"vision 처리량을 위해 S3에서 llama를 CPU로
  밀어냄"**을 허용. 정책 선택지:
  - LM을 "가능하면 항상 가속기"로 보장(QoS 우선) → **β≈0.75** 권장 (S3도 가속기로).
  - "vision 처리량 우선, LM best-effort" → **β=0.5 유지** 정당.
  → 이는 **데이터가 아니라 제품 정책** 결정. LM 지연이 vision 처리량 이득보다 중요한지에 대한 판단.
- **w1=1.0 (vision throughput)**: 기준 항. 유지.

**요약 제안**: 계수 변경 불필요가 기본. 단 **"LM은 가용 시 항상 가속기에 둔다"가 정책이면 β를
0.5→0.75로 상향**을 후속 작업으로 검토(영향은 NPU S3 한 세트 + 저부하 base 일부에 국한).
α는 현행 유지(레버 효과 미미). 모든 결정은 NPU에만 영향, GPU는 불변.

## 부록 — 방법·스크립트
- 15세트 × 2플랫폼 예측 캐시 후 인메모리 스윕 (`<scratchpad>/weights_review.py`).
- 측정 목적함수 비교 (측정 540창, 계수-무관 throughput 기준).
- 계수/코드/데이터 변경 없음. `predictions.csv`는 실행 후 원복.
